# BoostBot Starter Environment Variables

# Your Nostr private key (nsec format)
# Generate one at https://nostr-keygen.com/
NOSTR_BOOST_BOT_NSEC=nsec1your_actual_nsec_here

# Helipad webhook authentication token (optional)
# If set, webhook requests must include: Authorization: Bearer YOUR_TOKEN
HELIPAD_WEBHOOK_TOKEN=your_webhook_token_here

# Test mode - set to 'true' to log what would be posted without actually posting to Nostr
TEST_MODE=false

# Port for the webhook server (default: 3333)
PORT=3333

# Log level (ERROR, WARN, INFO, DEBUG)
LOG_LEVEL=INFO 